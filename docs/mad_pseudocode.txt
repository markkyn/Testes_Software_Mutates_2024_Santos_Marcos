function calculate_mad(values)
    number_of_values = length(values)

    total = 0

    for value in values
        total += value
    avg = total / number_of_values


    total = 0
    difference = 0
    for value in values
        difference = value - mean
        if difference is less than 0
            difference *= -1
        total += difference
    mad = total / number_of_values